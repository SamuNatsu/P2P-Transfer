/// Store module
import { createGlobalState } from '@vueuse/core';

// Export store
export const useStore = createGlobalState(() => {
  /// States

  /// Getters

  /// Actions

  /// Return store
  return {};
});
